<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="book.api.library">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookApiLibrary"
        android:usesCleartextTraffic="true">
        <activity android:name=".FavoriteBooksActivity"></activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterConfirmActivity" />
        <activity android:name=".RegisterActivity" /> <!-- Home -->
        <activity android:name=".HomeActivity"></activity> <!-- end -->
        <activity android:name=".ResultBookActivity" />
        <activity android:name=".VolumeInfoActivity" /> <!-- Main -->
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- end -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>